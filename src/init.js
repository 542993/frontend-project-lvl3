import render from "./view"


export default () => {
    const state = {
        formState: {
            valid: false,
            error: null,
            fields: {

            }
        },
        posts: [],
    }

}